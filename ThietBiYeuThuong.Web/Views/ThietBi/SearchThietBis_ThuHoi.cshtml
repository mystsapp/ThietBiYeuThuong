@model ThietBiViewModel

<div class="row form-group">
    <div class="col-xs-12 col-md-6">
        <div class="input-group">
            <input type="text" value="@Model.MaTBText" class="form-control form-control-sm txtThietBi_ThuHoi text-uppercase" id="txtThietBi_ThuHoi" placeholder="Nhập mã TB, tên TB">
            <span class="input-group-btn">
                <button type="button" class="btn btn-default btn-sm " id="btSearchTB_ThuHoi"><i class="fa fa-search"></i></button>
            </span>
        </div>
    </div>
</div>

<div style="overflow-x:auto; overflow-y:auto; height: 300px">
    <table class="table mytable text-nowrap text-sm" id="thietBiTbl">
        <thead>
            <tr class="table-info">

                <th>Mã TB</th>
                <th>Tên TB</th>
                <th>Mã BN</th>
                <th>Tên BN</th>
                <th>Ngày xuất</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.IEnumThietBi_ThuHoi != null)
            {
                @foreach (var item in Model.IEnumThietBi_ThuHoi)
                {

                    <tr class="thietbis-cursor-pointer ">

                        <td class="tdVal_TB" data-id="@item.ThietBiId">@Html.DisplayFor(m => item.ThietBiId)</td>
                        <td class="tdVal_TB" data-id="@item.ThietBiId">@Html.DisplayFor(m => item.ThietBi.TenTB)</td>
                        <td class="tdVal_TB" data-id="@item.ThietBiId">@Html.DisplayFor(m => item.BenhNhanId)</td>
                        <td class="tdVal_TB" data-id="@item.ThietBiId">@Html.DisplayFor(m => item.BenhNhan.HoTenBN)</td>
                        <td class="tdVal_TB" data-id="@item.ThietBiId">@Html.DisplayFor(m => item.NgayTao)</td>
                    </tr>

                }
            }
        </tbody>
    </table>
</div>
<!--<div class=" pt-1 pb-1">
    <button type="button" class="btn btn-success btn-sm"-->
@*id="btnKVCTPCTCreatePartial_Back"*@
<!--data-dismiss="modal"><i class="fas fa-backward "></i> Back</button>
</div>-->

<script>
    $('.btSearchTB_ThuHoi').off('click').on('click', function () {

        var maTBText = $('.txtThietBi_ThuHoi').val();

        ThietBiSearch_ThuHoi(maTBText); // ben Create_ThuHoi
    })
    $('.thietbis-cursor-pointer').click(function () {

        // giu trang thai
        if ($(this).hasClass("hoverClass"))
            $(this).removeClass("hoverClass");
        else {
            $('.thietbis-cursor-pointer').removeClass("hoverClass");
            $(this).addClass("hoverClass");
        }

        var maTB = $(this).children().eq(0).html();
        var tenTB = $(this).children().eq(1).html();
        var maBN = $(this).children().eq(2).html();
        var tenBN = $(this).children().eq(3).html();
        var ngayXuat = $(this).children().eq(4).html();

        $('#txtThietBi_ThuHoi').val(maTB);
        $('#txtTenTB_ThuHoi').val(tenTB);
        $('#hidMaBN').val(maBN);
        $('#txtDonVi_ThuHoi').val(tenBN);

        //var code = $(this).find("td").eq(0).html();
        //var name = $(this).find("td").eq(1).html();
        //var city = $(this).find("td").eq(2).html();
        //var address = $(this).find("td").eq(3).html();
        //var telephone = $(this).find("td").eq(4).html();
        //var email = $(this).find("td").eq(5).html();
        //var taxCode = $(this).find("td").eq(6).html();
        //var taxSign = $(this).find("td").eq(7).html();
        //var taxForm = $(this).find("td").eq(8).html();

        //console.log(text);
    })
</script>