@model ThietBiViewModel

<div class="row form-group">
    <div class="col-xs-12 col-md-6">
        <div class="input-group">
            <input type="text" value="@Model.MaTBText" class="form-control form-control-sm txtThietBi_ThemMoi text-uppercase" id="txtTimTB_ThemMoi" placeholder="Nhập mã TB, tên TB">
            <span class="input-group-btn">
                <button type="button" class="btn btn-default btn-sm " id="btSearchTB_ThemMoi"><i class="fa fa-search"></i></button>
            </span>
        </div>
    </div>
</div>

<div style="overflow-x:auto; overflow-y:auto; height: 300px">
    <table class="table mytable text-nowrap text-sm" id="thietBiTbl">
        <thead>
            <tr class="table-info">

                <th>Mã TB</th>
                <th>Tên TB</th>
                <th>Ngày nhập</th>
                <th>Trạng thái</th>
                <th>Loại TB</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.IEnumThietBi != null)
            {
                @foreach (var item in Model.IEnumThietBi)
                {

                    <tr class="thietbis-cursor-pointer ">

                        <td class="tdVal_TB" data-id="@item.MaTB">@Html.DisplayFor(m => item.MaTB)</td>
                        <td class="tdVal_TB" data-id="@item.MaTB">@Html.DisplayFor(m => item.TenTB)</td>
                        <td class="tdVal_TB" data-id="@item.MaTB">@Html.DisplayFor(m => item.NgayTao)</td>
                        <td class="tdVal_TB" data-id="@item.MaTB">@Html.DisplayFor(m => item.TrangThai.Name)</td>
                        <td class="tdVal_TB" data-id="@item.MaTB">@Html.DisplayFor(m => item.LoaiThietBi.Name)</td>
                    </tr>

                }
            }
        </tbody>
    </table>
</div>
<!--<div class=" pt-1 pb-1">
    <button type="button" class="btn btn-success btn-sm"-->
@*id="btnKVCTPCTCreatePartial_Back"*@
<!--data-dismiss="modal"><i class="fas fa-backward "></i> Back</button>
</div>-->

<script>
    $('.btSearchTB_ThemMoi').off('click').on('click', function () {

        var maTBText = $('.txtTimTB_ThemMoi').val();

        ThietBiSearch(maTBText); // ben Create
    })
    $('.thietbis-cursor-pointer').click(function () {

        // giu trang thai
        if ($(this).hasClass("hoverClass"))
            $(this).removeClass("hoverClass");
        else {
            $('.thietbis-cursor-pointer').removeClass("hoverClass");
            $(this).addClass("hoverClass");
        }

        var maBN = $(this).children().eq(0).html();
        var hoTenTN = $(this).children().eq(1).html();
        var sdt_TN = $(this).children().eq(2).html();
        var gt_TN = $(this).children().eq(3).html();
        var hoTenBN = $(this).children().eq(4).html();
        var namSinh = $(this).children().eq(5).html();
        var cmnd_CCCD_BN = $(this).children().eq(6).html();
        var diaChi = $(this).children().eq(7).html();

        $('#txtHoTenBN').val(hoTenBN);
        $('#txtHoTenTN').val(hoTenTN);
        $('#txtNamSinh').val(namSinh);
        $('#txtBenhNhanId_Create').val(maBN);

        //var code = $(this).find("td").eq(0).html();
        //var name = $(this).find("td").eq(1).html();
        //var city = $(this).find("td").eq(2).html();
        //var address = $(this).find("td").eq(3).html();
        //var telephone = $(this).find("td").eq(4).html();
        //var email = $(this).find("td").eq(5).html();
        //var taxCode = $(this).find("td").eq(6).html();
        //var taxSign = $(this).find("td").eq(7).html();
        //var taxForm = $(this).find("td").eq(8).html();

        //console.log(text);
    })
</script>